package com.kalo.notification.enums;

/**
 * Only one kind exists today. The enum is here so the second kind does not
 * require a migration, and so a partner screen can group by it.
 */
public enum NotificationType {
    RIDE_RATED
}
